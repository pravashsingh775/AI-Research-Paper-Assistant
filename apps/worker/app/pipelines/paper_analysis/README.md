# Paper analysis pipeline

Stages: object retrieval -> PDF extraction -> section detection -> page-aware chunking -> embeddings -> private namespace indexing. Preserve page, section, and character offsets for citations.
