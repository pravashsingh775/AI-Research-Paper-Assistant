Shared schemas belong here. Keep API DTOs and async event payloads versioned and provider-neutral.

Suggested contracts: `Paper`, `PaperChunk`, `SearchRequest`, `SearchResult`, `Citation`, `AnalysisJob`, and `EvidenceValidationResult`.
